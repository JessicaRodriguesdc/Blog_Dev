<div class="card mt-4">
    <div class="card-body">
        <h3>Categorias: </h3>
        <br>
        <hr>
        <br>
        {{#each categorias}}
            <a href="/categorias/{{slug}}"><h5>{{nome}}</h5></a>
        {{else}}
        {{/each}}
    </div>
</div>
<br>
<br>
<br>